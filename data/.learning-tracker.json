{
  "concepts": [
    {
      "id": "virtual-scroll-tree-flatten",
      "name": "虚拟滚动 + 树形扁平化（Virtual Scroll with Tree Flattening）",
      "category": "frontend",
      "description": "将递归 DOM 树迁移为扁平化数组 + react-window 虚拟滚动：用 useFlattenedTree hook 将嵌套的文件夹树递归展开为 FlatTreeItem[]，配合 useIndexTree 实现 O(1) 查找，解决大量文件夹节点导致的 DOM 性能问题",
      "learned_date": "2026-03-22",
      "source_commits": [
        "d33ac480 refactor(list-view): migrate from recursive DOM to virtual scroll using useIndexTree",
        "ffdca971 feat(list-view): add FlatTreeItem type and useFlattenedTree hook"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "dnd-kit-advanced-patterns",
      "name": "拖拽系统高级模式（@dnd-kit Advanced Patterns）",
      "category": "frontend",
      "description": "在 @dnd-kit 框架上实现复杂交互：drag-over 时文件夹高亮、500ms 悬停自动展开、自动滚动、拖拽选区偏移修正、drop target 精确判定、以及拖拽操作的 undo 支持",
      "learned_date": "2026-03-22",
      "source_commits": [
        "856d2018 feat: add folder group highlight on drag-over and enable file row drop targets",
        "e8d4b107 fix: resolve drag-drop issues in FinderListView"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "move-conflict-resolution-rollback",
      "name": "文件移动的冲突解决与失败回滚（Move Conflict Resolution & Rollback）",
      "category": "frontend",
      "description": "文件移动操作的容错设计：冲突时弹出 MoveConflictModal 让用户选择覆盖/跳过/重命名，部分失败时自动恢复被替换和被删除的文件到原位，以及 stop 行为的正确处理",
      "learned_date": "2026-03-22",
      "source_commits": [
        "3fc278cc feat(list-view): add move conflict resolution dialog and breadcrumb collapse"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "html-publish-workflow",
      "name": "网页发布流程设计（Web Page Publish Workflow）",
      "category": "frontend",
      "description": "完整的 HTML 文件发布到公网的流程：CustomPathDialog 支持用户自定义 URL 路径前缀、path validation、PublishButton 状态管理、SSE 过程中自动检查是否需要更新 publish、以及免费用户升级引导",
      "learned_date": "2026-03-22",
      "source_commits": [
        "b0d5cc50 feat: 初步完成 webpage publish",
        "dfa34ba6 feat(publish): PublishButton uses CustomPathDialog"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "websocket-reconnect-safety",
      "name": "WebSocket 重连安全机制（WebSocket Reconnect Safety）",
      "category": "frontend",
      "description": "WebSocket 连接的健壮性设计：离线检测（navigator.onLine + online/offline 事件）、重连时的竞态防护（防止多个重连同时进行）、指数退避重试、以及重试状态的 UI 反馈",
      "learned_date": "2026-03-22",
      "source_commits": [
        "a7882c84 fix: harden KuseClaw WebSocket with reconnect safety, offline detection, and retry UI"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "uiblock-interactive-cards",
      "name": "UIBlock 交互卡片模式（UIBlock Interactive Cards Pattern）",
      "category": "frontend",
      "description": "在 AI Chat 的 SSE 流中解析结构化 UI 指令（ask_user、suggest_connectors），渲染为交互卡片组件（AskUserCard、SuggestConnectorCard），用户操作后调用 ask_user/respond API 回传结果给后端 agent，实现 AI 驱动的多步交互流程",
      "learned_date": "2026-03-28",
      "source_commits": [
        "d8a095db feat: add UIBlock interactive cards (ask_user + suggest_connectors)",
        "d5bc5767 feat: UIBlock submit calls ask_user/respond API",
        "0bb593b4 fix: send response_key directly to ask_user/respond API",
        "8a657b9c feat: redesign AskUserCard — vertical list, custom input, skip"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "pipedream-connect-oauth",
      "name": "第三方 OAuth 集成（Pipedream Connect OAuth Integration）",
      "category": "backend",
      "description": "通过 Pipedream Connect SDK 实现第三方服务 OAuth 授权：后端 API route 生成 connect_token（绑定用户 ID 防止跨用户泄漏）、前端 PipedreamProvider 包裹 createFrontendClient、ConnectModal 触发 OAuth 弹窗、账户列表管理（连接/断开/重新授权）",
      "learned_date": "2026-03-28",
      "source_commits": [
        "a7f50f29 feat: integrate Pipedream Connect for connector OAuth flow",
        "f97628c9 feat: use real Pipedream data across all connector UI",
        "e2ab4e63 fix: prevent cross-user connector account leaks",
        "6adb5844 fix: resolve real user ID for Pipedream token creation",
        "10836fc5 fix: use correct cookie key and add error logging for user ID fetch",
        "33624a03 fix: connector UX — auto-close modal, fix disconnect, refetch accounts"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "customize-view-architecture",
      "name": "可定制化视图架构（Customize View Tab Architecture）",
      "category": "frontend",
      "description": "构建 Skills + Connectors 双标签页管理界面：SkillsTab 支持 Official/Custom 分类过滤、技能详情弹窗含文件树浏览器、技能的 CRUD 与 toggle 操作；ConnectorsTab 集成 Pipedream 的 curated apps 列表、搜索过滤、skeleton loading、lazy-mount tabs 优化首次渲染",
      "learned_date": "2026-03-28",
      "source_commits": [
        "3fc4e56c feat: add Customize page with Skills tab, card grid, and Create with Kuse flow",
        "ddfe9f5a feat: redesign skills UI with filter dropdown, card menus, and fix V2 API routing",
        "6de56090 feat: add Skill V2 integration with file tree dialog, metadata, and remove flow",
        "f59d9542 feat: tree view for skill file browser with expand/collapse directories",
        "29f44db1 feat: curated connectors list, SkillCard polish, and code cleanup",
        "97dd6da6 feat: polish Customize view UI",
        "b71c80e9 feat: skeleton loading, auto-close Pipedream success, lazy-mount tabs",
        "f7f9683d refactor: replace raw buttons with Button component, use Radix primitives"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "download-export-refactor",
      "name": "下载导出系统重构（Download/Export System Refactoring）",
      "category": "frontend",
      "description": "将分散在各组件中的下载逻辑统一到 FolderWidgets/Download 模块：按文件类型拆分 handler（fromMarkdown、fromHtml、fromPptx）、统一 PDF 导出管线（Tiptap → HTML → PDF，含 TOC、ref-link、heading 处理）、URL-based 下载选项抽象",
      "learned_date": "2026-03-28",
      "source_commits": [
        "dbeba01f download refactor",
        "66b5711d migration wip",
        "7a1ac35e fix headings",
        "96411fd3 fix ref link",
        "ac9f82d6 fix task list item",
        "4b3486c9 escape"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "credit-dispute-system",
      "name": "积分争议系统（Credit Dispute System）",
      "category": "general",
      "description": "完整的用户积分争议处理流程：DisputeDialog 弹窗让用户提交争议原因、API 层支持 dispute 的创建和状态查询（pending/approved/rejected）、Usage 面板集成争议状态图标、GA4 埋点追踪用户行为",
      "learned_date": "2026-03-28",
      "source_commits": [
        "0862515d feat: credit dispute UI - dialog, status icons, Usage panel integration",
        "9625b35f feat: credit dispute - tracking, UI polish, prompt field",
        "66ebf6a5 refactor: migrate dispute API paths from /credits/dispute to /dispute"
      ],
      "reviews": [
        {
          "date": "2026-03-28",
          "completed": true
        }
      ],
      "next_review": "2026-04-07",
      "review_stage": 1
    },
    {
      "id": "batch-operations-design",
      "name": "批量操作设计模式（Batch Operations Design Pattern）",
      "category": "frontend",
      "description": "设计文档驱动的 Trash 批量删除：区分清空全部和删除选中的独立端点、带 cleared_file_ids 的幂等 API 响应、store 层 Set<number> 选择管理、本地 Radix Dialog 确认、mutation flags 禁用并发操作",
      "learned_date": "2026-03-28",
      "source_commits": [
        "dbeac217 docs: add empty trash design and plan",
        "7677aebd feat: add empty trash user flow",
        "f72ced21 feat: replace empty trash confirm with radix dialog",
        "6f099788 feat: add trash selected delete data flow",
        "53585e92 feat: add trash selected delete ui",
        "f4f26e94 feat: 更新 trash 工具栏按钮样式及弹窗 UI"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "store/assets/index.ts (deleteItems)",
            "approach": "Finder delete: optimistic remove + backup nodesByPath/childrenByPath, UndoEntry for revert, global modal confirmation",
            "key_code_pattern": "Backup entire tree state → optimistic delete → on success create UndoEntry → on failure restore from backup"
          },
          {
            "location": "store/assets/index.ts (moveItems) + utils/api/files.ts",
            "approach": "Finder move: POST batched with file_ids, returns successfulIds + failedIds, per-item conflict dialog loop, partial rollback on failure",
            "key_code_pattern": "Optimistic rename → API returns success/fail split → restore failed items → emit ITEMS_MOVED event"
          }
        ],
        "comparison": "Trash delete uses separate endpoints (empty vs selected) with idempotent responses (cleared_file_ids), local Radix dialogs, no undo. Finder delete uses global modal, UndoEntry for revert, no detailed response. Finder move handles partial failure with per-item conflict resolution and rollback queue.",
        "trade_offs": "Trash: simpler (irreversible, no conflicts), idempotent, explicit confirmation. Finder delete: reversible via undo, optimistic for speed. Finder move: most complex but handles conflicts and partial failures gracefully.",
        "decision_guide": "Irreversible + no conflicts → Trash pattern (idempotent API, explicit dialog). Reversible + no conflicts → Finder delete (optimistic + UndoEntry). Reversible + may conflict → Move pattern (per-item resolution, partial rollback)."
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "race-condition-prevention-patterns",
      "name": "竞态条件防护模式集（Race Condition Prevention Patterns）",
      "category": "frontend",
      "description": "React + 异步工作流中防止竞态的五种模式：版本计数器（作废过时加载）、双重守卫（state + ref 去重）、布尔标志（并发防护）、AbortController（流取消）、定时器抑制（事件防抖）",
      "learned_date": "2026-03-28",
      "source_commits": [
        "1c5fec8c fix: type errors, duplicate upload button, new chat race condition, and nav switch bugs",
        "457754c9 fix: prevent stale history overwriting new conversation and drag-select deselection",
        "454210ba fix: clear sendToAgent payload in error catch to prevent duplicate sends",
        "c36b2a62 fix: remove duplicate plus icon in chat input area"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/hooks/useConversationManager.ts",
            "approach": "Version counter: loadVersionRef increments on createConversation, stale loads check and bail",
            "key_code_pattern": "const v = loadVersionRef.current; await fetch(); if (v !== loadVersionRef.current) return;"
          },
          {
            "location": "components/Chat/components/ChatInterface/index.tsx",
            "approach": "Dual guard: state null check + processedPayloadRef JSON.stringify dedup",
            "key_code_pattern": "if (!payload) return; const key = JSON.stringify(payload); if (key === ref.current) return;"
          },
          {
            "location": "components/CustomizeView/ConnectorsTab.tsx",
            "approach": "Boolean flag: connectingRef prevents concurrent OAuth popup opens",
            "key_code_pattern": "if (ref.current) return; ref.current = true; try {} finally { ref.current = false; }"
          },
          {
            "location": "components/Chat/hooks/useAIStream.ts",
            "approach": "AbortController: cancel SSE stream on conversation switch or unmount",
            "key_code_pattern": "abortControllerRef.current = new AbortController(); fetch(url, { signal: ref.current.signal })"
          },
          {
            "location": "components/FinderView/hooks/useListDragSelect.ts",
            "approach": "Timer suppression: suppressClickRef with armSuppressClick(ms) and re-arm on commitAndReset",
            "key_code_pattern": "armSuppressClick(200) on drag start; armSuppressClick(300) on commitAndReset"
          }
        ],
        "comparison": "Version counter invalidates stale async results without canceling them. Dual guard prevents duplicate processing of same payload across re-renders. Boolean flag blocks concurrent execution. AbortController cancels in-flight network operations. Timer suppression prevents post-transition events.",
        "trade_offs": "Version counter: simple, works for any async, but stale request still completes (wasted bandwidth). AbortController: cancels early, saves bandwidth, but more complex setup. Boolean flag: simplest, but blocks all concurrent calls. Dual guard: handles React's batching, but JSON.stringify has performance cost for large payloads.",
        "decision_guide": "Cancel network request → AbortController. Invalidate stale result → Version counter. Prevent duplicate processing → Dual guard. Block concurrent calls → Boolean flag. Suppress events during transitions → Timer suppression."
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "tiptap-inputrule-slash-commands",
      "name": "Tiptap InputRule 斜杠命令节点（Tiptap InputRule for Slash Commands）",
      "category": "frontend",
      "description": "通过 Tiptap InputRule 正则触发的自定义内联节点：用户输入 /skillName 加空格后自动替换为不可编辑的原子标签。关键修复：正则需包含尾随 \\s$（因为 InputRule 在字符插入后触发），范围计算用 indexOf 而非 endsWith",
      "learned_date": "2026-03-28",
      "source_commits": [
        "084f5372 fix: InlineSkillNode InputRule triggers on space after /command",
        "b32942b0 feat: add V2 fields to ApiSkill interface and transform"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/components/InputArea/PromptInput/extensions/TemplatePlaceholderNode.ts",
            "approach": "Manual command insertion (no InputRule), uses ReactNodeViewRenderer for interactive UI with name and filledValue attributes",
            "key_code_pattern": "addNodeView() { return ReactNodeViewRenderer(TemplatePlaceholderView); }"
          },
          {
            "location": "components/MarkdownOverlay/extensions/CustomImage.ts",
            "approach": "Block node from markdown parsing, standard HTML img rendering with async upload state tracking",
            "key_code_pattern": "Standard Node.create with HTML rendering, no InputRule, handles paste/upload lifecycle"
          },
          {
            "location": "components/MarkdownOverlay/extensions/TextStyleMarks.ts",
            "approach": "Mark extension using markInputRule() helper for text formatting like ==highlight==",
            "key_code_pattern": "markInputRule({ find: /pattern/, type: this.type })"
          }
        ],
        "comparison": "InlineSlashNode uses InputRule for auto-trigger on typing + atom node (non-editable span). TemplatePlaceholder uses manual command + ReactNodeViewRenderer (full React component). CustomImage is block-level with no InputRule. CustomHighlight is a Mark (text formatting) not a Node.",
        "trade_offs": "InputRule: automatic, great UX for typing patterns, but regex timing is tricky (fires after character insertion). ReactNodeViewRenderer: full React rendering power, but heavier and not auto-triggered. Marks: simplest for text formatting, but can't represent complex content.",
        "decision_guide": "Auto-trigger on typing pattern → InputRule + Atom Node. Needs interactive React UI → ReactNodeViewRenderer. Text formatting only → Mark extension. Block-level media → Standard HTML Node."
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "sse-streaming-architecture",
      "name": "SSE 流式通信三层架构（SSE Streaming 3-Layer Architecture）",
      "category": "frontend",
      "description": "Chat Panel 的完整 SSE 数据流转管线：三层架构 — StreamManager（连接控制/重试/TTL/AbortController）→ AIStreamProcessor（事件分发/Agent-SubAgent 聚合/Tool 流式处理）→ MessageBuilder（增量消息构建/Part 合并/UIBlock）。通过 AsyncGenerator yield 链式传递 ProcessedEvent，useAIStream hook 消费并更新 React 状态",
      "learned_date": "2026-03-30",
      "source_commits": [
        "d8a095db feat: add UIBlock interactive cards (ask_user + suggest_connectors)",
        "8a657b9c feat: redesign AskUserCard — vertical list, custom input, skip",
        "2df16ddd fix: use sentinel UID for temp conversation to prevent stale reload",
        "454210ba fix: clear sendToAgent payload in error catch to prevent duplicate sends",
        "67a92317 feat: add slash command menu (/) for quick access to Add files and Skills"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "packages/htmlEditor/api/refine.ts",
            "approach": "Simple SSE: 直接调用 @microsoft/fetch-event-source，单一 onmessage 回调处理所有事件，无分层、无重试、无 Agent 聚合",
            "key_code_pattern": "fetchEventSource(url, { onmessage(msg) { onMessage(msg.data); }, onerror(err) { throw err; } })"
          },
          {
            "location": "lib/fetchEventSource/index.ts + parse.ts",
            "approach": "自定义 fetchEventSource 库：基于 ReadableStream 的字节级 SSE 解析，支持 Last-Event-ID 重连、visibility change 自动重连、自定义 onopen 验证",
            "key_code_pattern": "getBytes(body) → getLines(bytes) → getMessages(lines) → onmessage(event)"
          },
          {
            "location": "components/Chat/utils/streamManager.ts",
            "approach": "连接管理层：AsyncGenerator 封装 fetchEventSource，管理 Request-ID/Last-Event-ID、指数退避重试（AUTO/MANUAL/RELOAD 策略）、TTL 过期降级、visibility retry、AbortController 可中断 sleep",
            "key_code_pattern": "async *streamWithRetry() { while(true) { yield* connectAndStream(); // 错误时分类 → 自动重试或 yield error } }"
          },
          {
            "location": "components/Chat/utils/streamProcessor.ts",
            "approach": "事件处理层：消费 AsyncGenerator<RawSSEEvent>，switch 分发 20+ 事件类型，管理 Agent/SubAgent 状态机、Tool 生命周期、文件流式预览、历史消息回放",
            "key_code_pattern": "async *processEventStream(eventStream) { for await (const event of eventStream) { yield* processEvent(event); } }"
          },
          {
            "location": "components/Chat/utils/messageBuilder.ts",
            "approach": "消息构建层：增量构建 Message 对象，基于 eventId 合并连续同类 Part，支持 text/thinking/coding/image/table/tool_call/ui_block/agent_block 等 11 种 Part 类型",
            "key_code_pattern": "appendText(content, type, eventId) { if (lastPart.eventId === eventId) merge; else push new; }"
          }
        ],
        "comparison": "项目中存在两套 SSE 方案：(1) htmlEditor/refine.ts 的简单方案：单层回调，无分层无重试，~50 行代码，适合一次性 AI 编辑请求；(2) Chat 系统的三层架构：StreamManager→AIStreamProcessor→MessageBuilder，~2500 行代码，支持多 Agent 嵌套、Tool 流式文件预览、断点续传、TTL 降级重载。核心差异在于 Chat 需要处理复杂的 Agent 层级（主 Agent + SubAgent）和 Tool 生命周期（start→stream→result→end），而 htmlEditor 只需要简单的文本流。",
        "trade_offs": "简单方案（refine.ts）：代码少、易维护、无状态管理开销，但不支持重试、不能处理多 Agent、不能做断点续传。三层架构（Chat）：功能完整、健壮、可扩展，但代码量大（2500+ 行）、调试复杂（事件要经过 3 层转换才到 UI）、新增事件类型需要修改 streamProcessor 的 switch。",
        "decision_guide": "单向文本流 + 一次性请求（如 AI 编辑）→ 简单方案（直接 fetchEventSource + 单回调）。多 Agent 嵌套 + Tool 流式 + 需要重试/断点续传 → 三层架构。介于两者之间（如需要重试但无 Agent 嵌套）→ 只用 StreamManager + 简单 processor，跳过 MessageBuilder 的复杂 Part 系统。"
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "scheduled-tasks-crud-architecture",
      "name": "定时任务 CRUD 架构（Scheduled Tasks CRUD Architecture）",
      "category": "frontend",
      "description": "从零构建完整的定时 AI 任务管理功能：API class 封装 REST 端点（PUT 状态更新 + PATCH 字段编辑的分离）、Zustand store 乐观更新 + toast 反馈、TaskCreateDialog/EditDialog 的 delta-diff 提交、SchedulePicker cron 表达式双向转换、FolderSelector 复用 Chat 模块的 FolderPickerDialog、ScrollablePopoverContent 解决 Radix Popover 滚动问题",
      "learned_date": "2026-04-04",
      "source_commits": [
        "28e029df feat: scheduled tasks UI — API client, store, components",
        "2f06c1aa feat: add Scheduled Tasks sidebar entry + view integration",
        "092b746b feat: SchedulePicker — user-friendly schedule editor",
        "4a18f745 feat: refactor ScheduledTasks with TaskCreateDialog and Tailwind migration",
        "9ca258f4 feat: enhance ScheduledTasks with calendar picker, task CRUD, and UI polish",
        "6e2e197e feat: redesign ScheduledTasks dialogs with Figma spec, add folder/model selectors",
        "22a99687 fix: UI review — dark mode, accessibility, responsive, token consistency",
        "1b292a7f feat: add task detail drawer in right panel slot",
        "a37281a7 ux: improve TaskCard feedback, empty state, and error display"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "store/trash/index.ts",
            "approach": "Mature Zustand store: subscribeWithSelector middleware, event bus integration (TRASH_UPDATED), AbortController for request cancellation, named selector exports (useTrashItems), getInitialState() for reset, granular loading flags (isRestoring, isDeleting, isEmptying)",
            "key_code_pattern": "create<T>()(subscribeWithSelector(...)) + emitStoreEvent(STORE_EVENTS.TRASH_UPDATED)"
          },
          {
            "location": "store/assets/index.ts",
            "approach": "Most complex store: tree data management, optimistic IDs, UndoEntry rollback, subscribeWithSelector + devtools middleware, fine-grained backup-and-restore on failure",
            "key_code_pattern": "Backup nodesByPath/childrenByPath → optimistic write → on failure restore from backup → emit typed events"
          },
          {
            "location": "components/Chat/components/WorkInFolder/FolderPickerDialog.tsx",
            "approach": "Full folder picker: virtualized tree dialog, inline NewFolderDialog with nested path support, calls registerExternalFolder after mkdir",
            "key_code_pattern": "FolderPickerDialog + NewFolderDialog → assetsStore.registerExternalFolder()"
          }
        ],
        "comparison": "useTurboTasksStore 是项目中最简单的 CRUD store：plain create() 无中间件、无事件总线、无 AbortController、无 named selector exports。与 useTrashStore 对比缺少 subscribeWithSelector（外部无法订阅细粒度变化）、request cancellation（快速切换可能导致 stale write）、getInitialState（无法 reset）。FolderSelector 复用了 FolderPickerDialog 但其内置 CreateFolderDialog 不调用 registerExternalFolder，新建文件夹不会立即出现在树中。SchedulePicker 的 ScrollablePopoverContent 解决了 Radix Popover 在 Dialog 内的 animation+scroll 冲突，是项目级可复用的方案。",
        "trade_offs": "简单 store（TurboTasks）：代码少、易理解、开发快，但缺少 request cancellation 和事件集成。成熟 store（Trash/Assets）：健壮但代码量大、学习曲线高。delta-diff edit 模式：只发送改变的字段，减少服务端写入，但需要 undefined vs null 语义约定。",
        "decision_guide": "新功能 MVP → 简单 store 模式（plain create + 乐观更新 + toast）。功能稳定后需要跨组件响应 → 升级到 subscribeWithSelector + event bus。有并发写入风险 → 加 AbortController。需要 undo → 参考 assetsStore 的 backup-restore 模式。Dialog 内有 Popover → 使用 ScrollablePopoverContent。"
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "ime-composition-state-guard",
      "name": "IME 输入法组合状态防护（IME Composition State Guard）",
      "category": "frontend",
      "description": "useCompositionState hook 实现 CJK 输入法的 Enter 键防护：双重检查（composingRef + nativeEvent.isComposing）防止浏览器竞态，ref 而非 state 避免重渲染。两种消费模式：组件内直接实例化 vs 父组件实例化后 props 下传（虚拟列表场景共享单一 ref）",
      "learned_date": "2026-04-04",
      "source_commits": [
        "c38f44bb fix: resolve IME composition Enter key bugs in rename inputs"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "hooks/useCompositionState.ts",
            "approach": "标准 hook：composingRef (useRef<boolean>) + handleCompositionStart/End + isComposing(e) 双重检查（ref || nativeEvent.isComposing）",
            "key_code_pattern": "composingRef.current || e.nativeEvent.isComposing"
          },
          {
            "location": "components/Chat/components/InputArea/PromptInput/index.tsx (line 83, 203-221)",
            "approach": "Tiptap/ProseMirror 环境：native DOM addEventListener（非 React synthetic event），单一 ref 检查（无 nativeEvent.isComposing），因为 ProseMirror handleKeyDown 收到的是原生 KeyboardEvent",
            "key_code_pattern": "el.addEventListener('compositionstart/end', ...); if (isComposingRef.current) return false;"
          },
          {
            "location": "components/Chat/components/WorkInFolder/index.tsx (line 121)",
            "approach": "内联检查：直接用 e.nativeEvent.isComposing，无 ref 备份，无 hook",
            "key_code_pattern": "if (e.nativeEvent.isComposing) return;"
          },
          {
            "location": "app/(dashboard)/components/AccountSettings.tsx (line 225)",
            "approach": "最老的模式：e.nativeEvent.isComposing || e.keyCode === 229，含 legacy Android keyCode 229 fallback",
            "key_code_pattern": "e.nativeEvent.isComposing || e.keyCode === 229"
          }
        ],
        "comparison": "项目中有 5 种 IME 防护层级：(1) useCompositionState hook 双重检查（最健壮）；(2) ProseMirror native DOM + 单 ref（Tiptap 必须用 native listener）；(3) TemplatePlaceholderView 手动双重检查（在 ProseMirror NodeView 中重新实现 hook 逻辑）；(4) 内联 nativeEvent.isComposing（简单但缺少 ref 备份）；(5) keyCode === 229 legacy fallback（最老，兼容旧 Android）。核心差异在于 React synthetic event vs native DOM event 的环境约束。",
        "trade_offs": "useCompositionState hook：最安全（双重检查），但需要 3 个 props 传递。内联 nativeEvent.isComposing：最简单，但在某些浏览器/OS 组合中 isComposing 可能在 keydown 时已经为 false（已知竞态）。ProseMirror native listener：唯一选择（ProseMirror 不走 React event），但需要 useEffect 清理。prop delegation 模式：虚拟列表中避免 N 个 ref 实例（只有一个同时在编辑），但让 TreeRowContentProps 承载了 IME 相关 props。",
        "decision_guide": "React 控制的 input/contentEditable（rename、search、modal）→ useCompositionState hook。Tiptap/ProseMirror 内部 → native DOM listener + ref。低风险输入（非提交型）→ 内联 nativeEvent.isComposing。虚拟列表中的编辑行（同一时间只有一个编辑）→ 父组件实例化 hook + props 下传。"
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "tiptap-suggestion-plugin",
      "name": "Tiptap Suggestion 插件（Slash Command Menu）",
      "category": "frontend",
      "description": "通过 Tiptap Suggestion 插件实现 Notion 风格的 / 斜杠命令菜单：ProseMirror plugin 监控输入触发字符、ReactRenderer 在 document.body 创建独立 React root 渲染下拉菜单、@floating-ui/dom 基于光标 VirtualElement 定位、PluginKey 状态共享实现 Enter 键冲突解决（编辑器的 Enter-to-submit 让步给菜单的 Enter-to-select）",
      "learned_date": "2026-04-04",
      "source_commits": [
        "67a92317 feat: add slash command menu (/) for quick access to Add files and Skills"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/components/InputArea/PromptInput/extensions/InlineSlashNode.ts (addInputRules)",
            "approach": "InputRule 模式：正则匹配完成的 /skillName + 空格后自动替换为 atom 节点。无 UI overlay，纯文本模式匹配",
            "key_code_pattern": "new InputRule({ find: /(?:^|\\s)\\/([^\\s/]+)\\s$/, handler: ... })"
          },
          {
            "location": "components/Chat/components/InputArea/PromptInput/extensions/TemplatePlaceholderNode.ts + View.tsx",
            "approach": "ReactNodeViewRenderer 模式：通过编程命令插入节点，节点内部是完整 React 组件（含 input、事件处理），需要 stopPropagation 防止 ProseMirror 拦截键盘事件",
            "key_code_pattern": "addNodeView() { return ReactNodeViewRenderer(TemplatePlaceholderView); } + native event listeners with stopPropagation"
          }
        ],
        "comparison": "三种 Tiptap 扩展模式各解决不同问题：Suggestion 是最重的方案（ProseMirror plugin + ReactRenderer + floating UI），但提供最好的发现性 UX（用户不需要记住命令名）。InputRule 是最轻的（纯正则替换），适合 power user 的快捷输入。ReactNodeView 是最灵活的（完整 React 渲染），适合文档内嵌入交互组件。Suggestion 和 InputRule 在本项目中互补：Suggestion 是发现入口，InputRule 是快捷入口，两者产生相同的 inlineSlash 节点。",
        "trade_offs": "Suggestion：完整的发现性 UX（popup + 搜索过滤 + 键盘导航），但需要手动管理 React 生命周期（ReactRenderer 创建独立 React root，不继承父应用的 Context）。InputRule：零 UI 开销，但用户必须知道命令名。ReactNodeView：全功能 React 渲染，但必须手动 stopPropagation 所有键盘事件，且所有事件必须用 native listener（ProseMirror 会先于 React 拦截 keydown）。",
        "decision_guide": "需要发现性菜单（搜索 + 选择列表）→ Suggestion plugin + ReactRenderer。需要自动文本替换（无 UI）→ InputRule。需要文档内嵌入 React 交互组件 → ReactNodeViewRenderer + native event interception。需要两者 → 同时注册（如本项目：Suggestion 是主路径，InputRule 是 fallback）。关键注意：ReactRenderer 的 React root 不继承 Context，需要全局 Provider（Lingui macro 可工作因为是编译时转换）。"
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "exclusive-view-state-pattern",
      "name": "互斥视图状态模式（Exclusive View State Pattern）",
      "category": "frontend",
      "description": "多布尔标志 + resetAllViews 集中清除函数实现视图互斥：6 个 is*View 标志通过 resetAllViews() 原子性清除再设置，保证同一时刻只有一个视图激活。配套的 auto-rename + scroll-into-view 模式：Store 事件 → subscribeToStoreEvent(FOLDER_CREATED) → requestAnimationFrame → selectMultiple + setEditingPath → scrolledForRef 去重滚动",
      "learned_date": "2026-04-04",
      "source_commits": [
        "abdf1927 feat: unify exclusive views into single activeView state and add task utils",
        "e3a4faca feat: auto-rename mode and scroll-into-view after New Folder creation"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "store/ui/index.ts (resetAllViews, lines 543-564)",
            "approach": "多布尔标志 + 集中清除：6 个 is*View boolean 通过 resetAllViews({ newFlag: true }) 原子性切换，早退优化（无活跃视图时跳过 set()）",
            "key_code_pattern": "resetAllViews(merge?) { if (!anyActive && !merge) return; set({ isTrash: false, isShared: false, ..., ...merge }); }"
          },
          {
            "location": "store/ui/index.ts (viewMode, line 49)",
            "approach": "字符串联合枚举：type ViewMode = 'blob' | 'list'，纯互斥无附带 metadata，TypeScript 编译时保证穷尽",
            "key_code_pattern": "type ViewMode = 'blob' | 'list'; set({ viewMode: 'list' })"
          },
          {
            "location": "components/FinderView/FinderView.tsx (lines 269-278)",
            "approach": "Store event 驱动的 auto-rename：subscribeToStoreEvent(FOLDER_CREATED) → pathDirname guard → requestAnimationFrame → selectMultiple + setEditingPath",
            "key_code_pattern": "subscribeToStoreEvent(FOLDER_CREATED, ({path}) => { rAF(() => { selectMultiple([path]); setEditingPath(path); }); })"
          },
          {
            "location": "components/FinderView/components/FinderListView.tsx (lines 379-392)",
            "approach": "去重滚动：scrolledForRef 防止同一 editingPath 重复滚动，可见性检查（只在 off-screen 时滚动），居中对齐（scrollTop = targetTop - clientHeight/2）",
            "key_code_pattern": "if (editingPath === scrolledForRef.current) return; scrolledForRef.current = editingPath; el.scrollTop = targetTop - el.clientHeight/2;"
          }
        ],
        "comparison": "项目中视图互斥有两种方案：(1) 多布尔 + resetAllViews：允许每个视图携带独立 metadata（customizeInitialTab、pendingShareToken、newTaskInitialPrompt），但 TypeScript 无法静态保证互斥。(2) ViewMode 字符串枚举：编译时穷尽检查，但不支持 per-state metadata。Auto-rename 的 store event 模式是一个跨层通信典型：assetsStore 创建文件夹 → emit FOLDER_CREATED → FinderView 订阅 → 进入编辑模式 → FinderListView 滚动到位，全程无 prop drilling。",
        "trade_offs": "多布尔 + enforcer：灵活（每个视图独立 metadata）、渐进式（新增视图只加一个 boolean + 在 resetAllViews 中清除），但无编译时互斥保证（直接 set() 可绕过 enforcer）。枚举/判别联合：编译时安全，但新增状态需修改 union type + 所有 switch/if 消费点。Store event auto-rename：解耦好（store 不关心 UI 如何响应），但事件链长（3 层跳转），调试需要追踪事件流。",
        "decision_guide": "2 种状态、无 metadata → boolean。3+ 种状态、无 per-state metadata → 字符串枚举（如 ViewMode）。3+ 种状态、部分带 metadata → 多布尔 + resetAllViews（当前方案）或判别联合 type（更安全但迁移成本高）。跨层 UI 响应（store action → 多个 UI 副作用）→ Store event pub-sub + subscribeToStoreEvent。"
      },
      "reviews": [],
      "next_review": "2026-04-05",
      "review_stage": 0
    },
    {
      "id": "subscription-downgrade-state-machine",
      "name": "订阅降级状态机（Subscription Downgrade State Machine）",
      "category": "frontend",
      "description": "完整的订阅降级生命周期管理：2 层状态架构（subscription 级 scheduled_change + component 级 pendingPaymentKey 锁）、scheduled/restore/current 渲染状态、结构化错误分类（DowngradeErrorKind）、GA4 漏斗埋点、团队 vs 个人差异化处理（feature flag + tenant 检查）",
      "learned_date": "2026-04-12",
      "source_commits": [
        "6bfb85f9 feat(pricing): add scheduled downgrade and restore plan UX",
        "ed880fa2 feat(billing): add downgrade API contracts and shared downgrade utils",
        "82e86123 feat(downgrade): enhance downgrade messaging and error handling",
        "b108c611 feat(billing): add team scheduled downgrade flow behind feature flag",
        "89f8aebc fix(billing): unify keep current plan wording for downgrade restore"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "apis/pages.ts (PublishStatus)",
            "approach": "文件发布状态：2 态（published/unpublished）+ dirty flag（has_changes），无调度器，变更即时生效",
            "key_code_pattern": "{ is_published: boolean, has_changes: boolean } — 单 API 调用 publish/unpublish"
          },
          {
            "location": "store/turboTasks/index.ts (TurboTask.status)",
            "approach": "定时任务状态：active/paused/deleted，PUT 端点切换状态，乐观更新 + toast 反馈",
            "key_code_pattern": "updateTaskStatus(id, status) → optimistic set → API PUT → rollback on error"
          },
          {
            "location": "components/Views/CreditsPlan/PricingCardContext.tsx",
            "approach": "PendingPaymentKey 锁模式：scoped key 防止双重提交，多选下拉兼容",
            "key_code_pattern": "setPendingPaymentKey(`personal:${priceLevel}:${billingPeriod}`) → await API → setPendingPaymentKey(null)"
          }
        ],
        "comparison": "项目中有三种生命周期模式：(1) 即时切换（Publish：变更立即生效，无 pending 状态）；(2) 乐观切换（TurboTask：先更新 UI 再等 API）；(3) 延迟调度（Downgrade：变更在账单周期结束时生效，期间可取消）。Downgrade 是唯一真正的“计划性状态转换”，核心区别在于引入了 effective_at 时间维度和 restore 回退路径。",
        "trade_offs": "即时切换：简单、反馈快，但不可逆。乐观切换：用户体验好，但需要 rollback 逻辑。延迟调度：安全（用户有反悔窗口），但 UI 复杂度高（需展示当前态 + 未来态 + 生效时间 + 取消按钮），错误处理更复杂（需要 error_code 分类）。",
        "decision_guide": "变更即时生效 + 可逆 → 乐观切换（如 task status toggle）。变更即时 + 不可逆 → 确认弹窗 + 即时切换（如 publish）。变更延迟生效 + 可取消 → 调度状态机（如 downgrade），需要额外的 scheduled 状态、effective_at 时间、restore 端点。涉及金钱/订阅 → 无乐观更新，全部等服务端确认。"
      },
      "reviews": [],
      "next_review": "2026-04-13",
      "review_stage": 0
    },
    {
      "id": "sse-event-driven-file-refresh",
      "name": "SSE 事件驱动文件树刷新（SSE Event-Driven File Tree Refresh）",
      "category": "frontend",
      "description": "SSE 流中的 deliverables/turn_end 事件通过 streamProcessor → mutationAdapter → refreshScheduler 管线触发文件树增量更新：即时 mutation（create/delete/rename 直接操作 nodesByPath）+ 延迟批量刷新（refreshScheduler 800ms 防抖 + seq 去重），Skills 列表通过 setTimeout 800ms 防抖自动刷新",
      "learned_date": "2026-04-12",
      "source_commits": [
        "8c18b535 fix: handle deliverables SSE event for turbo-created files in file tree",
        "30cbaacf feat(skills): auto-refresh skills list when skill files are created via chat",
        "6f36abe0 fix(assets): refresh file view after agent creates files"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/utils/fuse/refreshScheduler.ts",
            "approach": "批量防抖刷新：per-folder 800ms debounce + seq 序号去重，超时后校验 seq 未变化才执行 listDir()",
            "key_code_pattern": "schedule(path) { seq[path]++; clearTimeout(timers[path]); timers[path] = setTimeout(() => { if (seq[path] === currentSeq) listDir(path); }, 800); }"
          },
          {
            "location": "components/Chat/utils/fuse/mutationAdapter.ts",
            "approach": "即时 mutation：Fuse 文件事件直接操作 assets store 的 nodesByPath/childrenByPath，增量 treeVersion",
            "key_code_pattern": "ingestCreateFile(fileInfo) → nodesByPath[path] = node; childrenByPath[parent].push(path); treeVersion++"
          },
          {
            "location": "components/Chat/utils/streamProcessor.ts (line 1693)",
            "approach": "Skills 防抖刷新：全局 skillsRefreshTimer，800ms setTimeout，多次触发时 clearTimeout 合并为单次 loadSkills()",
            "key_code_pattern": "clearTimeout(skillsRefreshTimer); skillsRefreshTimer = setTimeout(() => loadSkills(), 800);"
          }
        ],
        "comparison": "项目中 SSE 副作用有三种模式：(1) 直接 store 更新（文件树 mutation：即时、无防抖，保证一致性）；(2) 防抖异步回调（Skills 刷新：800ms setTimeout 合并多次触发）；(3) 事件总线广播（STORE_EVENTS + emitStoreEvent：松耦合 UI 状态变更）。refreshScheduler 不走事件总线，而是直接依赖注入到 fuse handler。",
        "trade_offs": "即时 mutation：保证数据一致性，但每次 Fuse 事件都触发 React 重渲染。防抖批量刷新：减少 API 调用和渲染次数，但有 800ms 延迟。事件总线：松耦合但事件流难追踪。混合方案（本项目）：mutation 即时可见 + 刷新延迟批量，兼顾即时反馈和性能。",
        "decision_guide": "文件增删（必须即时可见）→ 直接 mutation（nodesByPath 操作）。文件夹内容列表刷新（可容忍延迟）→ refreshScheduler 防抖。第三方列表刷新（如 Skills）→ setTimeout 防抖。UI 状态变更（面板/选择）→ 事件总线。关键决策：delete 操作只做即时 mutation 不触发父目录刷新，避免删除后刷新导致已删文件闪现的竞态。"
      },
      "reviews": [],
      "next_review": "2026-04-13",
      "review_stage": 0
    },
    {
      "id": "cron-schedule-polling-architecture",
      "name": "Cron 调度与轮询架构（Cron Schedule & Polling Architecture）",
      "category": "frontend",
      "description": "SchedulePicker 的 cron 表达式双向转换（parseCron/buildCron 支持 6 种频率类型含 every-N-minutes 和 hourly interval）、多层自适应轮询（TaskList 10s/30s 按运行状态切换、TaskDetailPanel 5s/15s 按任务状态切换）、optimistic run 插入（合成 run_id: optimistic-{timestamp}）、变更检测跳过无效重渲染",
      "learned_date": "2026-04-12",
      "source_commits": [
        "06db98e4 feat: add hourly interval & every-N-minutes schedule types, improve task polling",
        "0dc4a1ea feat: improve scheduled tasks UX — optimistic runs, dedup status labels, cleanup",
        "d647136b feat: task runs API, real-time polling, and output display",
        "dfd5c093 ux(scheduled-tasks): add sticky Run History header and load more",
        "bfed7e97 fix: parse Unix timestamp (seconds) from backend for next_run"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/utils/streamManager.ts",
            "approach": "SSE 长连接 + 指数退避重试：AsyncGenerator 封装，支持 AUTO/MANUAL/RELOAD 重试策略",
            "key_code_pattern": "async *streamWithRetry() { while(true) { yield* connectAndStream(); await sleep(backoff); } }"
          },
          {
            "location": "store/assets/index.ts (pollForChanges)",
            "approach": "文件树增量轮询：比较 treeVersion 决定是否重新 listDir，无自适应间隔",
            "key_code_pattern": "setInterval(() => { if (treeVersion !== lastVersion) listDir(); }, interval)"
          },
          {
            "location": "components/ScheduledTasks/TaskList.tsx",
            "approach": "自适应间隔轮询：根据任务列表中是否有 running 状态切换 10s/30s 间隔",
            "key_code_pattern": "const interval = tasks.some(t => t.status === 'running') ? 10000 : 30000; setInterval(fetchTasks, interval)"
          }
        ],
        "comparison": "项目中有三种实时数据获取模式：(1) SSE 长连接（Chat：服务端推送，最实时但最重）；(2) 固定间隔轮询（Assets：简单但浪费带宽）；(3) 自适应轮询（ScheduledTasks：按状态调整间隔）。Cron 双向转换是项目中唯一的时间表达式与UI转换层。",
        "trade_offs": "SSE：最实时（毫秒级），但需要维护连接、处理断线重连。固定轮询：最简单，但在空闲时浪费带宽。自适应轮询：按需调整频率，但需要状态检测逻辑。Optimistic run：即时反馈，但需要 run_id 前缀去重。",
        "decision_guide": "服务端支持 SSE/WebSocket → 优先长连接。服务端只提供 REST API + 需要实时反馈 → 自适应轮询。服务端只提供 REST + 低实时要求 → 固定间隔轮询。用户触发的操作需要即时反馈 → optimistic 插入 + 去重。"
      },
      "reviews": [],
      "next_review": "2026-04-13",
      "review_stage": 0
    },
    {
      "id": "upload-replace-skill-crud",
      "name": "技能上传替换 CRUD（Upload/Replace Skill CRUD）",
      "category": "frontend",
      "description": "完整的文件上传到冲突检测到替换流程：UploadSkillDialog 支持拖拽/选择上传（.md/.zip/.skill）、客户端校验（类型+大小<10MB）、409 冲突检测（服务端返回已存在技能名）然后确认替换弹窗、forceReplace prop 跳过冲突确认、上传后 loadSkills() 全量刷新",
      "learned_date": "2026-04-12",
      "source_commits": [
        "e3c1600c feat: add upload skill feature (API, store, dialog, wiring)",
        "6cef35ea feat: add replace skill, improve upload dialog, remove write instruction",
        "5d25adfe fix: polish upload skill dialog and replace icon"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/utils/fileUpload.ts + utils/uploader.ts",
            "approach": "Chat 文件上传：多文件并行、临时占位符即时显示（optimistic）、uploadFilesUnified 带 lifecycle 回调（onProgress/onError）、部分失败处理、GA4 埋点、tempId到realId 映射",
            "key_code_pattern": "files.map(f => ({ tempId, name: timestamp_name })) → optimistic insert → parallel upload → map tempId to response"
          },
          {
            "location": "components/FinderView/FinderView.tsx (drag-and-drop upload)",
            "approach": "Finder 列表拖拽上传：onDrop → filterValidFiles → uploadFilesUnified → assets store refresh",
            "key_code_pattern": "onDrop(files) → uploadFilesUnified(files, folderId) → listDir(currentPath)"
          }
        ],
        "comparison": "Chat 上传是最复杂的（多文件并行、临时占位、进度追踪、部分失败粒度化处理）。Skills 上传是最简单的（单文件、无进度、全量刷新）。Finder 拖拽上传介于两者之间。核心差异：Chat 上传需要即时反馈（用户在聊天中等待），Skills 上传容忍全量刷新延迟。",
        "trade_offs": "单文件全量刷新（Skills）：代码最少（~50 行 API + dialog），但上传后 UI 有短暂空白。多文件 optimistic（Chat）：用户体验最好但代码量 10 倍以上。409 冲突检测模式：服务端驱动的冲突解决，比前端预检查更可靠但多一次 round-trip。",
        "decision_guide": "单文件上传 + 容忍短暂延迟 → 简单模式（API + loadAll 刷新）。多文件 + 需要即时反馈 → optimistic 模式（tempId + parallel upload + 映射）。有名称冲突可能 → 409 冲突检测 + 确认弹窗 + force 参数。大文件/需要进度 → uploadFilesUnified + onProgress 回调。"
      },
      "reviews": [],
      "next_review": "2026-04-13",
      "review_stage": 0
    },
    {
      "id": "newtask-conversation-history",
      "name": "NewTaskView 对话历史集成（NewTaskView Conversation History Integration）",
      "category": "frontend",
      "description": "在 NewTaskView 着陆页集成瀑布流历史对话列表：复用 ChatHeader 的 ConversationHistory 组件（currentConversation 传 null）、paginationOffsetRef + hasMoreRef 双 ref 去重并发 loadMore、cancelled 标记防止 unmount 写入、optimistic 本地更新（delete/rename 先改本地再 API）、paginationOffsetRef 删除后 -1 保持偏移对齐",
      "learned_date": "2026-04-14",
      "source_commits": [
        "db689ca4 feat: add conversation history to NewTaskView with switch, delete, rename support"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/components/ChatHeader/ConversationHistory/index.tsx",
            "approach": "Chat 内使用：currentConversation 非 null（高亮当前），外层传 delete/rename 回调。组件内部负责 popup、edit 编辑态、delete 确认弹窗",
            "key_code_pattern": "<ConversationHistory currentConversation={current} ... /> in Chat header"
          },
          {
            "location": "components/NewTaskView/index.tsx (lines 81-143)",
            "approach": "NewTaskView 新入口：currentConversation=null（无高亮）、paginationOffsetRef + hasMoreRef 绕过 closure stale、cancelled flag 保护 unmount、delete 后 offset -= 1",
            "key_code_pattern": "paginationOffsetRef.current = nextConversations.length; handleDeleteConversation → offsetRef -= 1"
          },
          {
            "location": "apis/folderConversation.ts",
            "approach": "waterfall 模式 API：{ limit, offset, mode: 'waterfall' } 返回 has_more + data；非分页用默认 mode",
            "key_code_pattern": "getFolderConversations({ limit: 20, offset, mode: 'waterfall' })"
          }
        ],
        "comparison": "同一个 ConversationHistory 组件被两种上下文复用：(1) Chat 场景：有 currentConversation 需高亮、父组件已维护 list/isLoading 状态；(2) NewTaskView 场景：无当前对话、父需自己管理 fetch + pagination + optimistic update。关键区别在于 NewTaskView 首次挂载时没有现成的对话列表，需要自己走一遍 fetch-on-mount + waterfall 分页模式。paginationOffsetRef 模式是标准的 ref-based 防并发写入方案。",
        "trade_offs": "ref 双重保护（isLoadingMoreRef + hasMoreRef）：可靠阻止并发，但需要手动同步 ref 和 state。cancelled flag 模式：简单（无 AbortController），但 fetch 仍会完成（浪费带宽）。optimistic update（先改本地）：UX 更快，但失败时需要 rollback（当前代码未处理）。复用 ConversationHistory 组件：代码复用率高，但组件的 props 接口必须支持两种上下文。",
        "decision_guide": "首页/着陆页需要展示对话列表 → 复用 ConversationHistory + 父自管 fetch 逻辑（currentConversation=null）。需要分页 + 防并发 → ref + state 双存（ref 用于 closure，state 用于 UI）。无 AbortController 的 fetch 保护 → cancelled flag（简单但浪费带宽）。delete/rename → optimistic 更新 + API 调用，失败当前忽略（可接受因为是幂等操作）。"
      },
      "reviews": [],
      "next_review": "2026-04-19",
      "review_stage": 0
    },
    {
      "id": "background-vs-item-context-menu",
      "name": "背景 vs 条目双上下文菜单（Background vs Item Context Menu Pattern）",
      "category": "frontend",
      "description": "Finder 右键菜单的分派策略：data-finder-item closest() 判断命中条目还是空白、两套独立 state（ctxMenu + bgCtxMenu）、右键未选中条目时自动 replace-select、多个 [data-context-menu] 标记并用 querySelectorAll 做统一 outside-click 关闭、selectedSetRef 避免 closure 捕获过时选择",
      "learned_date": "2026-04-14",
      "source_commits": [
        "bd6641fd feat: add right-click context menu for unselected items and empty space"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/AssetsTree/index.tsx (lines 274-541)",
            "approach": "Tree 侧边栏：单一 contextMenu state，每个 TreeRow 通过 onContextMenu 传 path 回调，setContextMenu({ open, x, y, path })",
            "key_code_pattern": "handleContextMenu(e, path) → setContextMenu({ open: true, x, y, path })"
          },
          {
            "location": "components/FinderView/FinderView.tsx (prior impl)",
            "approach": "旧版：单一 ctxMenu state，右键未选中时 early return（if selectedShapeIds.length === 0 return）",
            "key_code_pattern": "if (selectedShapeIds.length === 0) return; setCtxMenu({...})"
          },
          {
            "location": "components/FinderView/FinderView.tsx (bd6641fd new impl)",
            "approach": "新版：双 state（ctxMenu + bgCtxMenu）、closest('[data-finder-item]') 判定命中、未选中时 selectMultiple([path]) 自动选中、selectedSetRef 避免 closure 过时、querySelectorAll 多菜单 outside-click",
            "key_code_pattern": "const itemEl = e.target.closest('[data-finder-item]'); if (itemEl) { if (!selectedSetRef.current.has(path)) selectMultiple([path]); setCtxMenu(...); } else setBgCtxMenu(...);"
          }
        ],
        "comparison": "项目中有三种右键菜单模式：(1) Tree 单菜单 + path 参数（每 row 独立 onContextMenu，菜单内容统一）；(2) Finder 旧版单菜单 + selection gate（必须先选中才能右键，丢失空白区菜单场景）；(3) Finder 新版双菜单 + DOM 探测（同一处理器分派 item/background，支持未选中场景）。核心升级：从 state-driven 判定（selection.length）改为 DOM-driven 判定（closest data-attribute），覆盖更多场景。",
        "trade_offs": "单菜单 + path 参数（Tree）：最简单，但只能处理命中 row，无法区分空白。单菜单 + selection gate（旧 Finder）：实现快，但 UX 差（空白右键什么也不发生，违反 macOS/Windows 惯例）。双菜单 + DOM 探测（新 Finder）：UX 完整（覆盖 item/empty/multi-select 三场景），但需要两个 state、两个关闭逻辑、多菜单 outside-click（querySelectorAll）。selectedSetRef 模式：解决 React closure 捕获过时 selectedSet 的问题，是必需的 ref 模式。",
        "decision_guide": "只需要条目上下文菜单（如 tree row）→ 单菜单 + path 参数。右键空白需要不同菜单（如 Finder 的 New folder / Paste）→ 双菜单 + data-attribute 探测。右键未选中条目应该先选中再显示菜单 → selectMultiple 在 handleContextMenu 内调用（注意用 selectedSetRef 而非 selectedSet 避免过时）。多个菜单可能同时存在 → querySelectorAll('[data-context-menu]') + for 循环判定 contains。"
      },
      "reviews": [],
      "next_review": "2026-04-19",
      "review_stage": 0
    },
    {
      "id": "localstorage-view-state-persistence",
      "name": "localStorage 视图状态持久化模式（localStorage View State Persistence Pattern）",
      "category": "frontend",
      "description": "Zustand store 初始化时从 localStorage 读取视图布尔值，使 refresh 不丢失视图选择：getInitialNewTaskView() + typeof window 判定 SSR、三态读取（null=首次默认/'true'/'false'）、在 resetAllViews 原子写入、配合 KUSE_CLAW_VIEW 的删除式清理策略",
      "learned_date": "2026-04-14",
      "source_commits": [
        "22f27034 fix: persist isNewTaskView to localStorage so folder view survives page refresh",
        "db689ca4 feat: add conversation history to NewTaskView with switch, delete, rename support"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "store/ui/index.ts (getInitialFinderSortMode, getInitialListSortColumn, etc.)",
            "approach": "设置类（sort/zoom/viewMode）：typeof window === 'undefined' 返回默认值，否则读 localStorage 校验后返回。在 set() 时额外 localStorage.setItem 同步",
            "key_code_pattern": "const getInitialX = () => { if (typeof window === 'undefined') return DEFAULT; const saved = localStorage.getItem(KEY); return valid(saved) ? saved : DEFAULT; }"
          },
          {
            "location": "store/ui/index.ts (getInitialKuseClawView, line 59-62)",
            "approach": "布尔视图类（KuseClaw）：简单 === 'true' 判定，退出视图时 removeItem 删除键而非写 'false'",
            "key_code_pattern": "getInitial: () => localStorage.getItem(KEY) === 'true'; exit: localStorage.removeItem(KEY)"
          },
          {
            "location": "store/ui/index.ts (getInitialNewTaskView, 22f27034)",
            "approach": "布尔视图类（NewTask）：三态判定（null 首次默认 true, 'true', 'false'），退出时写 'false' 而非 removeItem（默认态要明确反向）",
            "key_code_pattern": "getInitial: () => { const saved = localStorage.getItem(KEY); return saved === null ? true : saved === 'true'; }"
          },
          {
            "location": "store/ui/index.ts (resetAllViews, 05349bd5)",
            "approach": "集中写入：在 resetAllViews 中根据 merge?.isNewTaskView 写 'true'/'false'，保证原子性（与其他 view 状态同步）",
            "key_code_pattern": "if (merge?.isNewTaskView) localStorage.setItem(KEY, 'true'); else localStorage.setItem(KEY, 'false');"
          }
        ],
        "comparison": "项目中 localStorage 持久化有四种模式：(1) 枚举设置（sort mode）：需要校验有效值；(2) 数值设置（zoom）：需要 parseFloat + 范围校验；(3) 布尔视图-默认 false（KuseClaw）：用 removeItem 清理；(4) 布尔视图-默认 true（NewTaskView）：必须写 'false' 明确覆盖默认。核心差异在默认值：首次用户应该处于哪个状态？KuseClaw 默认关闭（高级功能），NewTask 默认开启（着陆页）。",
        "trade_offs": "removeItem 清理（KuseClaw 模式）：localStorage 更干净，但默认必须是 false（否则删除后又变 true）。显式 'false' 写入（NewTask 模式）：localStorage 条目永存，但支持默认 true（首次用户体验）。SSR 保护（typeof window）：必须加，否则 Next.js SSR 时崩溃。集中写入 vs 分散写入：集中（resetAllViews）保证与其他 view 状态同步，分散写入（直接在 set 里 localStorage.setItem）简单但易漏。",
        "decision_guide": "默认开启的视图（着陆页类）→ 三态判定（null 默认 true）+ 写入 'false' 覆盖。默认关闭的视图（高级功能类）→ 简单判定（=== 'true'）+ removeItem 清理。有多个 view 互斥 → 在 resetAllViews 集中写入保证原子。配置类（非视图开关）→ 读时校验 + 写时直接 setItem。Next.js SSR → 永远加 typeof window === 'undefined' 保护。"
      },
      "reviews": [],
      "next_review": "2026-04-19",
      "review_stage": 0
    },
    {
      "id": "ttl-based-resource-ensure",
      "name": "TTL 周期性资源校验（TTL-Based Resource Ensure Pattern）",
      "category": "frontend",
      "description": "ensureDefaultFolder 用 60s TTL 定期重新校验默认文件夹是否存在，替代 one-shot flag。解决场景：用户在其他客户端/设备上删除了默认文件夹，one-shot 模式会永远不再重新创建；TTL 模式保证最迟 60s 后恢复",
      "learned_date": "2026-04-15",
      "source_commits": [
        "47d03304 refactor: use TTL-based re-check for ensureDefaultFolder instead of one-shot flag"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/Chat/components/WorkInFolder/index.tsx (ensureDefaultFolder, 47d03304)",
            "approach": "TTL 模块级变量：let lastEnsuredAt = 0; const ENSURE_TTL = 60_000; 每次调用判断 Date.now() - lastEnsuredAt > TTL",
            "key_code_pattern": "if (Date.now() - lastEnsuredAt > ENSURE_TTL) { await mkdir(...); lastEnsuredAt = Date.now(); }"
          },
          {
            "location": "components/Chat/utils/fuse/refreshScheduler.ts",
            "approach": "防抖调度：800ms debounce + seq 去重，每次 schedule() 重置 timer",
            "key_code_pattern": "clearTimeout(timers[path]); timers[path] = setTimeout(() => listDir(path), 800);"
          },
          {
            "location": "components/Chat/utils/streamManager.ts (TTL check)",
            "approach": "TTL 过期降级：SSE 连接 TTL 检查决定重连还是 reload 策略",
            "key_code_pattern": "if (Date.now() - connectedAt > TTL) return 'RELOAD'"
          },
          {
            "location": "apis/request.ts (token refresh patterns)",
            "approach": "缓存 + 过期重取：auth token 缓存到 sessionStorage，过期时重取",
            "key_code_pattern": "if (cachedToken && Date.now() < expireAt) return cachedToken; else fetch new;"
          }
        ],
        "comparison": "项目中有三类时间相关模式：(1) 防抖（debounce）：多次触发合并为一次，timer 重置，适合用户输入/批量刷新；(2) TTL 周期校验：首次触发后 TTL 内跳过，过期再做，适合“保证资源存在”类场景；(3) 过期降级：检测到过期时切换策略，适合连接级别 fallback。ensureDefaultFolder 从 one-shot flag（boolean）升级到 TTL（timestamp），核心驱动是“外部系统可能改变状态”这个假设。",
        "trade_offs": "One-shot flag：代码最少（一个 boolean），但假设“成功后状态不会变”，外部改变后无法恢复。TTL 模式：代码稍多（时间戳 + 比较），但允许外部变化恢复，缺点是 TTL 内的外部变化会有 60s 延迟被发现。轮询模式：最激进（固定间隔检查），保证最新但浪费 API 调用。Cache-then-invalidate：精确（明确清空），但需要 cache key 管理。",
        "decision_guide": "资源“创建一次后不变”（如 session id）→ one-shot flag。资源“应该存在但可能被外部删除”（如 default folder）→ TTL 模式，TTL 选择依据外部变化频率（60s 对用户操作足够）。数据“需要最新”（如实时 count）→ 轮询或 SSE。缓存“可以明确清空”（如用户操作后 invalidate）→ cache-then-invalidate。关键问题：一致性要求 vs 调用成本。"
      },
      "reviews": [],
      "next_review": "2026-04-19",
      "review_stage": 0
    },
    {
      "id": "centralized-view-side-effect",
      "name": "视图切换副作用集中化（Centralized View-Switch Side Effect）",
      "category": "frontend",
      "description": "把“切换到互斥视图时关闭 file preview”这个副作用从多个调用点搬到 resetAllViews 内部，新增 keepPreview opt-out 选项。配套 hide() 的 no-op guard 避免重复触发 React 渲染。模式体现：副作用应与触发动作原子化，opt-out 比 opt-in 更安全",
      "learned_date": "2026-04-15",
      "source_commits": [
        "05349bd5 fix: hide file preview when switching to exclusive views (Trash, Shared with me, etc.)"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "store/ui/index.ts (resetAllViews, 05349bd5)",
            "approach": "集中副作用：resetAllViews 内部直接调用 useFilePreviewStore.getState().hide()，通过 options.keepPreview 实现 opt-out",
            "key_code_pattern": "resetAllViews(merge, options) { if (!options?.keepPreview) filePreviewStore.hide(); set({...}); }"
          },
          {
            "location": "components/AssetsTree.tsx (05349bd5 prior)",
            "approach": "原本做法：在 enterTrashView、enterSharedWithMeView 等各调用点重复 filePreviewStore.hide()，容易漏写",
            "key_code_pattern": "enterTrashView 内：resetAllViews({...}); filePreviewStore.hide(); (每处都要写)"
          },
          {
            "location": "store/filePreview/store.ts (hide no-op guard)",
            "approach": "hide() 内部加 no-op 判断：if (!state.isVisible) return; 避免重复 setState 触发无意义渲染",
            "key_code_pattern": "hide: () => { if (!get().isVisible) return; set({ isVisible: false, ... }); }"
          },
          {
            "location": "store/ui/index.ts (resetAllViews, 提前返回)",
            "approach": "resetAllViews 本身也有 no-op guard：无活跃视图且无 merge 时提前 return，避免无谓 set()",
            "key_code_pattern": "if (!merge && !state.isTrashView && !state.isSharedWithMeView && ...) return;"
          }
        ],
        "comparison": "副作用管理有三种模式：(1) 调用点手动触发（旧做法）：每个 enterXXXView 都要 filePreviewStore.hide()，容易漏；(2) 集中触发 + opt-in（风险模式）：需要每次明确声明要关闭，漏声明=保留，bug 倾向保留预览；(3) 集中触发 + opt-out（当前做法）：默认关闭，特殊场景显式 keepPreview=true，漏声明=安全（关闭）。no-op guard 是另一维度：避免重复 set() 导致的渲染浪费，适用于频繁调用的 action。",
        "trade_offs": "调用点手动：最灵活但最易漏。集中 opt-in：显式但默认行为不安全。集中 opt-out：默认安全但需要 opt-out 机制。no-op guard：性能优化，但逻辑略复杂（需要先 get() 判断）。",
        "decision_guide": "多个调用点应该触发同一副作用 → 移到被调用方（store action 内部），不是调用方。默认行为安全方向是“执行副作用”→ opt-out（keepPreview=true 豁免）。默认行为是“跳过副作用”→ opt-in（需要显式 forceHide=true 才执行）。action 可能被重复调用且 set() 开销大 → 加 no-op guard。关键启发：subscribeWithSelector 下，set() 即使值不变也可能触发订阅者 rerender，no-op guard 是必要优化。"
      },
      "reviews": [],
      "next_review": "2026-04-19",
      "review_stage": 0
    },
    {
      "id": "per-user-vs-device-storage",
      "name": "客户端存储的数据归属：账号级 vs 设备级（Per-user vs Device-global Client Storage）",
      "category": "general",
      "description": "区分一个 localStorage 值是设备/浏览器属性还是账号属性：引用服务端实体（folder id、置顶、版本偏好）的数据必须按 userId 分键、账号切换时重新同步、并一次性迁移旧的全局 key；纯 UI 开关（如展开状态）保持设备级",
      "learned_date": "2026-06-08",
      "source_commits": [
        "6f99f305 fix(pinned): scope pinned-project storage to the signed-in user",
        "389ffdd6 refactor(pinned): use shared localStorage helpers from utils/storage"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "utils/versionPreference.ts",
            "approach": "Kuse 1.5/2.0 版本偏好按 userId 分键（kuse_version_preference_<userId>），首次无用户读取时懒迁移旧全局 key 到 guest 桶",
            "key_code_pattern": "USER_STORAGE_PREFIX + userId；按需 pull 读取 + CustomEvent 跨组件同步"
          }
        ],
        "comparison": "versionPreference 用函数按需 pull + window CustomEvent 同步；pinnedProjects 把数据镜像进 Zustand store，订阅 user store 在账号变化时 push 重载。两者都按 userId 分键、都有 legacy 迁移、都有 guest 桶。差异在读取/同步模型：pin 驱动实时渲染列表，需要 store push 避免账号切换后的旧 UI；version preference 只在路由决策时读一次，pull 足够。",
        "trade_offs": "设备级 key：简单，但共享浏览器配置下会跨账号泄漏服务端数据。账号级 key：杜绝泄漏，但需要迁移逻辑 + 账号切换重同步，复杂度更高。store 镜像 push vs 按需 pull：前者保证 UI 实时正确但要维护订阅，后者轻量但派生 UI 会有旧值风险。",
        "decision_guide": "数据引用服务端实体（id/账号）→ 按 userId 分键 + 迁移旧 key + 账号变化重同步；纯 UI 开关 → 设备级 key 即可。若该数据驱动实时渲染的列表/UI → 镜像进 store 并在账号变化时 push（避免旧 UI）；否则按需从 storage pull 足够。上线分键改动时务必做一次性 legacy 迁移，否则老用户上线即丢数据。"
      },
      "reviews": [],
      "next_review": "2026-06-09",
      "review_stage": 0
    },
    {
      "id": "surviving-remounts-cache-strategies",
      "name": "组件重挂载存活的三种缓存策略（Surviving Component Remounts: Module Cache vs Store vs Event-Invalidation）",
      "category": "frontend",
      "description": "Radix PopoverContent 关闭即卸载，导致内部列表组件每次打开都重挂载重拉数据。修复在一个文件里用了三种缓存：① 模块级缓存 + 会话标志（无 store 的原始 API 数据）② 复用 Zustand store + 会话标志（已有 store 的数据）③ store 事件原地打补丁（保持缓存新鲜，不轮询）",
      "learned_date": "2026-06-08",
      "source_commits": [
        "b9f25050 perf(sidebar): skip refetch when reopening the My workflows popover"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "store/workflow (useWorkflowStore) + MyWorkflowsList.tsx turbo path",
            "approach": "turbo workflows 已存在 Zustand store，store 天然跨重挂载存活，只需 hasFetchedTurboWorkflowsOnce 会话标志挡掉每次打开的冗余 fetchWorkflows",
            "key_code_pattern": "store cache + session flag guard"
          },
          {
            "location": "MyWorkflowsList.tsx storeEvents.subscribe(MA_WORKFLOW_CHANGED / RUN_STATE / STATUS_CHANGED / LAST_RUN_CHANGED)",
            "approach": "chat 流监测到 workflow 变化时发 store 事件，订阅者把 cachedMAWorkflowProjects 原地 map 打补丁，保持模块缓存新鲜而不重新请求",
            "key_code_pattern": "event-driven in-place cache patching"
          }
        ],
        "comparison": "三种策略针对三种数据源同居一个文件：① MA workflows 来自无 store 的原始 API → 用模块级变量 cachedMAWorkflowProjects（最便宜的「比重挂载活得久」容器）；② turbo workflows 已在 store → 复用 store + 会话标志；③ 所有缓存的新鲜度 → store 事件原地打补丁。关键区分：『跨重挂载存活』(①②) 和『保持新鲜』(③) 是两个独立问题。",
        "trade_offs": "模块缓存：最简单、跨重挂载、但跨标签页不共享且页面刷新即失。store 缓存：复用已有基础设施、自动响应式。事件失效：实时新鲜、不轮询、但要为每种变化手写订阅（本例 4 个）。轮询/禁用缓存是反模式（浪费请求/退化体验）。",
        "decision_guide": "数据已在 store 里 → 复用 store + 会话标志挡每次打开的请求；无 store → 模块级缓存。缓存在别处打开时可能变旧 → 加事件驱动的原地打补丁（不重新请求）；不会变旧 → 存活缓存即可。永远不要把『存活』和『新鲜』混为一谈。"
      },
      "reviews": [],
      "next_review": "2026-06-09",
      "review_stage": 0
    },
    {
      "id": "disciplined-shared-selector-extraction",
      "name": "有纪律的共享选择器抽取：按规则抽而非按关键词（Disciplined Shared-Selector Extraction）",
      "category": "frontend",
      "description": "PinnedSection 和 ProjectsPopover 共享同一条「按 pin 顺序拆分 pinned+余项」的业务规则，抽成通用 splitByPinned(items, pinnedIds, getId)，用 getId adapter 适配不同 item 形状；而 AssetsTree 只需布尔 includes() 成员判断（形状不同、无收益），刻意保留 inline。体现项目规则『按共享业务规则抽取，不为行数指标抽取』",
      "learned_date": "2026-06-08",
      "source_commits": [
        "64cb368e refactor(pinned): share splitByPinned between sidebar and popover",
        "fa6b35f2 refactor(assets-tree): share Pin/Share menu item builders via hooks",
        "e77e9f41 refactor(assets-tree): extract shared InlineRenameInput for tree + pinned rows"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "components/AssetsTree/index.tsx (pinned membership check)",
            "approach": "只需判断单个节点是否置顶：pinnedIds.includes(getNodeNumericId(node))——成员判断，不是有序拆分",
            "key_code_pattern": "boolean includes() membership test"
          },
          {
            "location": "components/AssetsTree/widgets/projectMenuItems.tsx (Pin/Share menu builders) + InlineRenameInput",
            "approach": "同周另两个抽取：Pin/Share 菜单项构造、tree+pinned 行共享的内联重命名输入框——都是真正共享的逻辑",
            "key_code_pattern": "shared hook / shared component extraction"
          }
        ],
        "comparison": "splitByPinned 抽取的是『pin 顺序身份解析』这条规则（用 Map 按 id 索引、再按 pinnedIds 顺序映射），两个消费方通过 getId 参数适配各自 item 形状。AssetsTree 共享的只是『置顶』这个概念/词，其逻辑（单节点布尔判断）本质不同，强行合并会得到更差的抽象。判别器：『共享同一业务规则，还是只共享同一个词？』",
        "trade_offs": "抽取：消除真实重复、规则单点维护、改一处全生效。但过度抽取（把形状不同的逻辑塞进一个泛化 helper）会增加参数/分支、降低可读性，比少量重复更糟。项目 CLAUDE.md 明确：不要仅为满足行数指标拆文件。",
        "decision_guide": "2+ 处共享同一条业务规则（非仅关键词）→ 抽通用选择器 + adapter 参数（如 getId）；只共享词/概念但逻辑不同 → 保留 inline；仅为命中行数指标 → 不抽。InlineRenameInput / projectMenuItems 是『真共享』的正例，AssetsTree.includes() 是『假共享』保留 inline 的正例。"
      },
      "reviews": [],
      "next_review": "2026-06-09",
      "review_stage": 0
    },
    {
      "id": "version-counter-derived-store-read",
      "name": "版本计数器信号驱动派生 store 读取（Version-Counter Signal for Derived Store Reads）",
      "category": "frontend",
      "description": "当 Zustand selector 返回每次新建的数组/对象（如 getRootFolders()），直接订阅会因引用不稳定导致过度渲染。改为订阅一个只在真正变化时自增的整数 treeVersion，在以它为 key 的 useMemo 内用 getState() 命令式读取派生数据；store 还选择性 bump（无关排序的元数据更新不自增）",
      "learned_date": "2026-06-08",
      "source_commits": [
        "4052c1b9 feat(sidebar): group pinned projects at the top of ProjectsPopover (ProjectsPopover treeVersion read)"
      ],
      "deep_analysis": {
        "existing_implementations": [
          {
            "location": "store/assets/index.ts (treeVersion counter)",
            "approach": "treeVersion 整数计数器，在 create/delete/rename 时自增；元数据更新仅当排序相关字段变化才 bump（bumpVersion 条件）",
            "key_code_pattern": "selective version counter as 'did anything I care about change' signal"
          },
          {
            "location": "store/pinnedProjects + store/assets 使用 subscribeWithSelector",
            "approach": "两个 store 都用 subscribeWithSelector 中间件，支持细粒度选择器订阅",
            "key_code_pattern": "subscribeWithSelector middleware"
          }
        ],
        "comparison": "朴素写法 useAssetsStore(s => s.getRootFolders()) 每次返回新数组，Zustand 用 Object.is 比较永远不相等 → 组件每次 store 变化都重渲染。本周写法：订阅稳定整数 treeVersion，useMemo([treeVersion]) 内 getState().getRootFolders() 命令式读取，仅在计数器变化时重算。额外精细度：store 选择性 bump，无关排序的元数据写入不触发 popover 重算。",
        "trade_offs": "订阅派生数组：写法直观，但引用不稳定 → 过度渲染，需自定义 equality 才能驯服。计数器信号 + getState()：精确控制重算时机、可选择性 bump，但需要 store 维护计数器（你得拥有该 store）。退路：无法改 store 时给 selector 传自定义 equality（如 useShallow）。",
        "decision_guide": "selector 返回新建的对象/数组 → 不要直接订阅；订阅版本计数器 + useMemo 内 getState() 读取（你拥有 store 时）。selector 返回原始值/稳定引用 → 直接订阅即可。无法给不属于你的 store 加计数器 → 给 selector 传自定义/shallow equality fn 作为退路。"
      },
      "reviews": [],
      "next_review": "2026-06-09",
      "review_stage": 0
    }
  ],
  "last_updated": "2026-06-08",
  "weeks": [
    {
      "week": "2026-W12",
      "summary": "FinderListView 虚拟滚动重构、拖拽系统增强、HTML Publish 功能、文件分享、WebSocket 重连加固",
      "concepts_learned": [
        "virtual-scroll-tree-flatten",
        "dnd-kit-advanced-patterns",
        "move-conflict-resolution-rollback",
        "html-publish-workflow",
        "websocket-reconnect-safety"
      ],
      "commits_analyzed": 90
    },
    {
      "week": "2026-W13",
      "summary": "Customize View 全新页面、Pipedream OAuth 集成、UIBlock 交互卡片、下载导出重构、积分争议系统、Trash 批量删除、Chat 竞态修复、Tiptap InlineSlashNode",
      "concepts_learned": [
        "uiblock-interactive-cards",
        "pipedream-connect-oauth",
        "customize-view-architecture",
        "download-export-refactor",
        "credit-dispute-system",
        "batch-operations-design",
        "race-condition-prevention-patterns",
        "tiptap-inputrule-slash-commands"
      ],
      "commits_analyzed": 95,
      "learning_recommendations": [
        {
          "topic": "React Concurrent Rendering & Race Conditions",
          "why": "Fixed 4 race condition bugs using 5 ad-hoc patterns — need systematic mental model for when React re-renders cause async overlap",
          "how": "Read React docs 'Synchronizing with Effects' race conditions section; compare all 5 patterns side-by-side; use React DevTools Highlight Updates during rapid conversation switching",
          "priority": "high"
        },
        {
          "topic": "Tiptap/ProseMirror Plugin Architecture",
          "why": "Fixed InputRule regex bug by pattern-matching, not understanding ProseMirror's transaction model — need to predict timing issues, not debug them",
          "how": "Read ProseMirror inputrules package source (~200 lines); add console.log to observe state.doc content at InputRule handler time",
          "priority": "medium"
        },
        {
          "topic": "Backend Batch Operation Patterns",
          "why": "W13 was 100% frontend (8 concepts, 0 backend). Designed trash delete API contract but only touched frontend. Understanding server-side transactions, soft-delete vs hard-delete, bulk optimization would improve API design skills",
          "how": "Check trash delete endpoint server implementation; research database transaction patterns for bulk operations; consider GDPR implications of soft-delete (trash_cleared_at) vs hard-delete",
          "priority": "medium"
        }
      ]
    },
    {
      "week": "2026-W14",
      "summary": "Scheduled Tasks 完整 CRUD 功能、IME 输入法 Enter 键 bug 修复、Slash Command 菜单（Tiptap Suggestion）、互斥视图状态 resetAllViews 模式、auto-rename + scroll-into-view、Insurance landing page、SSE 三层架构深度分析",
      "concepts_learned": [
        "sse-streaming-architecture",
        "scheduled-tasks-crud-architecture",
        "ime-composition-state-guard",
        "tiptap-suggestion-plugin",
        "exclusive-view-state-pattern"
      ],
      "commits_analyzed": 70,
      "learning_recommendations": [
        {
          "topic": "Zustand Store 成熟度升级模式",
          "why": "本周 useTurboTasksStore 是最简单的 CRUD store（plain create, 无中间件），对比 useTrashStore/useAssetsStore 缺少 subscribeWithSelector、AbortController、event bus。需要建立从 MVP store 到 production store 的升级路径",
          "how": "读源码：对比 store/turboTasks/index.ts（~130 行）和 store/trash/index.ts（~300 行）的差异；做实验：给 turboTasksStore 添加 subscribeWithSelector，观察是否有外部需要订阅的场景；看文档：Zustand v4 middleware composition 文档",
          "priority": "high"
        },
        {
          "topic": "ProseMirror Plugin State 与 React 状态同步",
          "why": "Slash Command 用 SLASH_SUGGESTION_KEY.getState(view.state) 在 ProseMirror handleKeyDown 中读取插件状态，实现 Enter 键冲突解决。这个 ProseMirror PluginKey 状态读取是理解 Tiptap 扩展间通信的关键",
          "how": "读源码：@tiptap/suggestion 包的 suggestion.ts（~200 行），关注 PluginKey 如何 set/get 状态；做实验：在 PromptInput handleKeyDown 中 console.log 所有 PluginKey 状态",
          "priority": "medium"
        },
        {
          "topic": "Radix UI 组件在嵌套场景的陷阱",
          "why": "本周发现 Radix Popover 在 Dialog 内的 animation 阻塞 overflow scroll 问题，用 ScrollablePopoverContent 解决。Radix 组件的 Portal/Focus trap/Animation 在嵌套场景经常出问题",
          "how": "读源码：SchedulePicker.tsx 的 ScrollablePopoverContent 实现；系统性测试：Dialog 内嵌 Popover、Popover 内嵌 Select、Dialog 内嵌 AlertDialog 的焦点和滚动行为；看 Radix GitHub issues 标签 'portal' 和 'focus-trap'",
          "priority": "medium"
        },
        {
          "topic": "跨浏览器 IME 事件时序",
          "why": "useCompositionState 的双重检查是为了防止特定浏览器/OS 组合中的竞态。但目前对 Chrome/Firefox/Safari 各自的 composition event 时序差异缺乏系统理解",
          "how": "做实验：在 Chrome、Firefox、Safari 中分别用中文输入法测试 compositionstart → keydown → compositionend 的事件顺序和 isComposing 值；看规范：W3C UI Events spec 的 Composition Events 章节",
          "priority": "low"
        }
      ]
    },
    {
      "week": "2026-W15",
      "summary": "订阅降级状态机、SSE 事件驱动文件树刷新、Cron 调度与轮询架构、技能上传替换 CRUD",
      "concepts_learned": [
        "subscription-downgrade-state-machine",
        "sse-event-driven-file-refresh",
        "cron-schedule-polling-architecture",
        "upload-replace-skill-crud"
      ],
      "commits_analyzed": 105,
      "learning_recommendations": [
        {
          "topic": "Zustand 中间件组合与 Store 成熟度升级",
          "why": "W14 已识别 turboTasksStore 是最简 store，W15 新增 4 个 store 交互场景，需要建立从 MVP 到 production store 的系统性升级路径",
          "how": "读源码：对比 store/pro/index.ts 和 store/turboTasks/index.ts 的中间件差异；做实验：给 turboTasksStore 添加 subscribeWithSelector + event bus；看文档：Zustand v4 middleware composition docs",
          "priority": "high"
        },
        {
          "topic": "前端计费系统的设计模式",
          "why": "本周实现完整 downgrade 流程但对 Stripe/支付后端理解有限。pending/scheduled/active 状态机模式在 SaaS 计费中是通用的",
          "how": "读 Stripe 文档：Subscription Schedules API 和 Proration 章节；对比项目 API 的 scheduled_change_effective_at 与 Stripe 的 schedule.phases",
          "priority": "medium"
        },
        {
          "topic": "SSE 断点续传与重连策略的系统对比",
          "why": "项目中 SSE 三层架构和 deliverables 事件刷新都依赖可靠的 SSE 连接，但 Last-Event-ID 重连、visibility change 重连、TTL 降级的细节理解不够深",
          "how": "读源码：lib/fetchEventSource/index.ts 和 streamManager.ts 的重连逻辑；做实验：Chrome DevTools 模拟网络断开到恢复，观察 Last-Event-ID 传递；看 MDN：EventSource reconnection 规范",
          "priority": "medium"
        }
      ]
    },
    {
      "week": "2026-W16",
      "summary": "NewTaskView 对话历史集成、右键双上下文菜单（item + background）、localStorage 视图状态持久化、TTL 资源校验、视图切换副作用集中化，以及 FolderPickerDialog 搜索/默认值/布局改进",
      "concepts_learned": [
        "newtask-conversation-history",
        "background-vs-item-context-menu",
        "localstorage-view-state-persistence",
        "ttl-based-resource-ensure",
        "centralized-view-side-effect"
      ],
      "commits_analyzed": 17,
      "learning_recommendations": [
        {
          "topic": "状态机 + localStorage 持久化的系统化抽象",
          "why": "本周新增 NEW_TASK_VIEW_STORAGE_KEY，项目里已有 7+ 个独立的 localStorage 键（sort mode/zoom/view mode/list sort column/list sort direction/kuse claw view/new task view），都是手写 getInitial + set 同步。每增加一个视图都是重复劳动，容易漏 SSR 保护或写入同步",
          "how": "读源码：store/ui/index.ts 所有 STORAGE_KEY 的使用（约 7 处），找出重复模式；做实验：抽象 createPersistedAtom<T>(key, default, validator) helper，把 7 个 getInitial 和对应 set 逻辑迁移；看 Zustand 文档：官方 persist middleware，对比手写 vs middleware 的取舍（partialize 控制字段、storage adapter、onRehydrateStorage 时机）",
          "priority": "high"
        },
        {
          "topic": "Context Menu / Popup Positioning 的系统性方案",
          "why": "本周手写了 x/y 计算 + 边界夹取（Math.min(x, innerWidth - 216)），还写了自己的 outside-click 机制（querySelectorAll）。项目里已有 Radix Popover、@floating-ui，但 FinderView 没用 — 因为右键位置需要跟随鼠标而非 trigger 元素。这类 cursor-anchored popup 需要更系统的方案",
          "how": "读源码：@floating-ui/react 的 VirtualElement 用法（Slash Command 菜单已用）；做实验：把 FinderView 的 ctxMenu 改造为 @floating-ui VirtualElement，观察是否能统一 outside-click 和碰撞检测；读 docs：Radix DropdownMenu.Trigger asChild 模式，看是否支持程序化触发",
          "priority": "medium"
        },
        {
          "topic": "后端视角的 Default Resource 管理",
          "why": "ensureDefaultFolder 的 TTL 模式是前端在补偿“后端没有 idempotent create default folder” 的缺陷。如果后端返回的 user profile 里总是包含 default folder id，前端根本不需要 ensure。W16 再次 100% 前端，缺少后端视角",
          "how": "对比：前端 TTL ensure vs 后端 idempotent GET /me（返回 default_folder_id）；读项目的 user info endpoint 响应结构，找是否已有 default_folder 字段；思考：如果改为后端保证，前端代码能简化多少（估计可以删掉 ensureDefaultFolder 和整个 TTL 逻辑）；看 REST API 设计最佳实践：idempotent initialization patterns",
          "priority": "medium"
        },
        {
          "topic": "副作用与 Zustand selector 渲染边界",
          "why": "hide() 的 no-op guard 和 resetAllViews 的 early return 说明 Zustand set() 即使值不变也会触发 subscribeWithSelector 的订阅者重渲染。缺少对 Zustand selector equality check 机制的系统理解（shallow vs Object.is），导致要手动加 guard",
          "how": "读源码：Zustand 的 createImpl.ts 中 setState 的 equality check 逻辑；做实验：给 hide() 去掉 no-op guard，用 React DevTools Profiler 测量预览组件的 rerender 次数；看文档：Zustand 的 selector + equalityFn 参数（第二参数可传 shallow 或 Object.is）",
          "priority": "low"
        }
      ]
    },
    {
      "week": "2026-W24",
      "summary": "新增置顶项目侧边栏功能 + 三个收起态侧栏弹窗（Projects/Recents/My workflows）；核心工程决策是底层 plumbing：客户端存储的账号级分键、组件重挂载的缓存存活、有纪律的共享逻辑抽取、版本计数器驱动派生读取",
      "concepts_learned": [
        "per-user-vs-device-storage",
        "surviving-remounts-cache-strategies",
        "disciplined-shared-selector-extraction",
        "version-counter-derived-store-read"
      ],
      "commits_analyzed": 230,
      "learning_recommendations": [
        {
          "topic": "后端 session/账号级数据隔离（本周存储分键的服务端对应）",
          "why": "本周全是前端；置顶泄漏 bug 根源是数据在客户端。需理解后端如何做租户隔离/防越权——碰到了 user_id/tenant_id 字段却没碰执行层",
          "how": "读 Go 后端注入认证用户到请求上下文的中间件；追踪一个 list 端点从 handler→query→按租户过滤的 WHERE；实验：去掉租户过滤看泄漏什么",
          "priority": "high"
        },
        {
          "topic": "Zustand selector 性能与 equality 函数（系统化）",
          "why": "treeVersion 只是诸多防过度渲染模式之一，你用对了但是临时手搓；还有 useShallow/自定义 equality/subscribeWithSelector/原子选择器",
          "how": "读 Zustand 多切片选择 + useShallow 文档；审计两个已用 subscribeWithSelector 的 store；用 Profiler 对比 treeVersion 写法 vs useShallow 写法的渲染次数",
          "priority": "high"
        },
        {
          "topic": "成熟缓存失效模式（React Query/SWR）vs 手搓事件",
          "why": "MyWorkflowsList 手搓模块缓存 + 4 个事件订阅保新鲜，这正是 TanStack Query 的失效+乐观更新；了解成熟模型才能判断何时手搓合理",
          "how": "读 TanStack Query 的 Query Invalidation + Optimistic Updates 文档，把本周代码逐项映射（cache=query cache、事件 handler=setQueryData、skip-flag=staleTime）",
          "priority": "medium"
        },
        {
          "topic": "Radix UI primitives 的 mount/portal 生命周期",
          "why": "本周两个 bug 都源于 Radix 行为：弹窗关闭即卸载、Dialog 作兄弟节点渲染以免 portal 点击冒泡误导航",
          "how": "读 Radix forceMount/Portal 文档；按 MyWorkflowsList 注释故意复现点击冒泡导致误导航的 bug",
          "priority": "low"
        }
      ]
    }
  ],
  "pages": [
    {
      "filename": "week-2026-W12.html",
      "title": "Week 12 Learning Hub",
      "type": "weekly",
      "generated_date": "2026-03-22",
      "concepts": [
        "virtual-scroll-tree-flatten",
        "dnd-kit-advanced-patterns",
        "move-conflict-resolution-rollback",
        "html-publish-workflow",
        "websocket-reconnect-safety"
      ]
    },
    {
      "filename": "week-2026-W13.html",
      "title": "Week 13 Learning Hub",
      "type": "weekly",
      "generated_date": "2026-03-28",
      "concepts": [
        "uiblock-interactive-cards",
        "pipedream-connect-oauth",
        "customize-view-architecture",
        "download-export-refactor",
        "credit-dispute-system",
        "batch-operations-design",
        "race-condition-prevention-patterns",
        "tiptap-inputrule-slash-commands"
      ]
    },
    {
      "filename": "learn-react-race-conditions.html",
      "title": "React Race Conditions: Systematic Mental Model",
      "type": "topic",
      "generated_date": "2026-03-28",
      "concepts": [
        "race-condition-prevention-patterns"
      ]
    },
    {
      "filename": "week-2026-W14.html",
      "title": "Week 14 Learning Hub — CRUD Architecture, IME Guards, Tiptap Suggestion & View State Patterns",
      "type": "weekly",
      "generated_date": "2026-04-04",
      "concepts": [
        "sse-streaming-architecture",
        "scheduled-tasks-crud-architecture",
        "ime-composition-state-guard",
        "tiptap-suggestion-plugin",
        "exclusive-view-state-pattern"
      ]
    },
    {
      "filename": "week-2026-W15.html",
      "title": "Week 15 Learning Hub — Downgrade State Machine, SSE Refresh, Cron Polling & Skill Upload",
      "type": "weekly",
      "generated_date": "2026-04-12",
      "concepts": [
        "subscription-downgrade-state-machine",
        "sse-event-driven-file-refresh",
        "cron-schedule-polling-architecture",
        "upload-replace-skill-crud"
      ]
    },
    {
      "filename": "learn-zustand-store-maturity.html",
      "title": "Zustand Store Maturity — Middleware Composition & Upgrade Patterns",
      "type": "topic",
      "generated_date": "2026-04-12",
      "concepts": [
        "scheduled-tasks-crud-architecture"
      ]
    },
    {
      "filename": "week-2026-W16.html",
      "title": "Week 16 Learning Hub — Conversation History, Dual Context Menus, Persistence Patterns & TTL Ensure",
      "type": "weekly",
      "generated_date": "2026-04-18",
      "concepts": [
        "newtask-conversation-history",
        "background-vs-item-context-menu",
        "localstorage-view-state-persistence",
        "ttl-based-resource-ensure",
        "centralized-view-side-effect"
      ]
    },
    {
      "filename": "week-2026-W24.html",
      "title": "Week 24 Learning Hub",
      "type": "weekly",
      "generated_date": "2026-06-08",
      "concepts": [
        "per-user-vs-device-storage",
        "surviving-remounts-cache-strategies",
        "disciplined-shared-selector-extraction",
        "version-counter-derived-store-read"
      ]
    }
  ]
}